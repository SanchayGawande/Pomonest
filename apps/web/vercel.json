{
  "framework": "nextjs",
  "buildCommand": "cd ../../packages/shared-timer && pnpm install --no-frozen-lockfile && pnpm build && cd ../../apps/web && SKIP_ENV_VALIDATION=true pnpm build",
  "outputDirectory": ".next",
  "installCommand": "pnpm install --no-frozen-lockfile"
}